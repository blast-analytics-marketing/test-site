# AEP Test Site Data Layer

## Overview

This site implements the **Adobe Client Data Layer (ACDL)**.

All page-level and interaction-level data is pushed using:

```javascript
window.adobeDataLayer.push()

Test Site Data Layer
Overview

This site implements the Adobe Client Data Layer (ACDL).

All page-level and interaction-level data is pushed using:

window.adobeDataLayer.push()

The data layer follows an event-based architecture, where each push contains:

An event key (required)

Contextual data relevant to that event